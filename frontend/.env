## (Opcional) URL base absoluta da API.
## Na maioria dos casos locais (Windows), NÃO defina esta variável: o código usará proxy (package.json -> "proxy": "http://localhost:5002").
## Defina apenas se precisar apontar para outra origem (por ex: ambiente Codespaces ou deploy).
# REACT_APP_API_URL=https://seu-host-5002.app.github.dev
